package main

func main() {
	//sak := sakura.Builder{}.Build()
	//_ = sak.User("user/ischenkx").Subscribe(context.Background(), "chat/123")
	//_ = sak.Topic("chat/123").Publish(context.Background(), []byte("Hello World!"))
	//
	//pubsub := sak.Broker().PubSub()
	//_ = pubsub.Subscribe(context.Background(), sak.Topic("chat/123").Channel())
	//
	//ch, _ := pubsub.Channel(context.Background())
	//
	//for ev := range ch {
	//
	//}
}
