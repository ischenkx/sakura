syntax='proto3';

message DNSRequest {
  // something for future
  string Token = 1;
}

message DNSResponse {
  string Address = 1;
}

service DNSLoadBalancer {
  rpc GetAddress(DNSRequest) returns (DNSResponse);
}